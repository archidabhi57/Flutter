import 'dart:io';

void main(List<String> args) {
  List<int> numbers = [];
  while (true) {
    int a = int.parse(stdin.readLineSync()!);
    int odd = 0, even = 0;
    for (int i = 0;; i++) {
      if (a == 0) {
        break;
      } else {
        if (a > 0) {
        } else {}
      }
    }
  }
}
